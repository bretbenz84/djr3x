# Cross-platform runtime dependencies for DJ-R3X.
#
# Keep this file to direct dependencies only. Pinning a full `pip freeze`
# snapshot makes fresh installs brittle across macOS and Raspberry Pi because
# many transitive packages publish different wheels per platform.
#
# Raspberry Pi note: prefer `pip install -r requirements-raspberry-pi.txt`
# so pip can use piwheels for packages like dlib.

# Shared runtime dependencies
dlib==20.0.1
elevenlabs==2.41.0
holidays==0.94
numpy==2.4.4
openai==2.30.0
opencv-python==4.13.0.92
openwakeword==0.4.0
pyserial==3.5
python-dotenv==1.2.2
requests==2.33.1
scipy==1.17.1
sounddevice==0.5.5
soundfile==0.13.1

# Apple Silicon-only local transcription stack
huggingface_hub==1.10.1; platform_system == "Darwin" and platform_machine == "arm64"
mlx==0.31.1; platform_system == "Darwin" and platform_machine == "arm64"
mlx-metal==0.31.1; platform_system == "Darwin" and platform_machine == "arm64"
mlx-whisper==0.4.3; platform_system == "Darwin" and platform_machine == "arm64"
