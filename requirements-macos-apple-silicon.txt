# Apple Silicon install wrapper.
#
# Build dlib in a clean Apple Silicon environment after upgrading pip,
# setuptools, and wheel. If dlib has trouble finding libpng, install
# Homebrew's `libpng` first and export:
#   PKG_CONFIG_PATH=/opt/homebrew/lib/pkgconfig

dlib==20.0.1
-r requirements.txt
